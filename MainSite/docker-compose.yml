services:
  web:
    build: .
    ports:
      - "8080:8080"
    environment:
      PERSIST_DIR: /var/www/site/persist
    volumes:
      - ./volumes:/var/www/site/persist
    restart: unless-stopped
